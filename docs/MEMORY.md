# MEMORY - Estado Atual do Projeto

Este arquivo registra apenas o estado vigente, as frentes ativas e os próximos passos imediatos do **βetaFleet**.
Histórico detalhado de sessões anteriores: `docs/MEMORY-HISTORY.md`.

## Estado Atual

- **Pagamentos — INSERT de parcela exige status pendente e auditoria vazia (2026-09-12; migration `20260912000000` **aplicada em DEV e em PROD pelo usuário**, diagnóstico estrutural conforme nas 15 cláusulas nos dois ambientes, **comportamento validado por E2E real em DEV (4/4)**; **em produção**)**: encerra a pendência de PRIORIDADE ALTA aberta em 2026-09-10. A policy `payment_installments_insert` passou a exigir `status = 'pendente_aprovacao'` e os sete campos de auditoria vazios (`payment_approved_by/at`, `paid_by/at`, `cancelled_by/at`, `cancellation_reason`). Antes disso qualquer papel com porta de INSERT (Fleet Assistant+, Admin Master, Workshop na própria OS) podia criar parcela já `aprovado` ou `pago` mandando o campo direto à API, pulando a aprovação do Coordenador — brecha aberta desde 2026-07-08 e **nunca explorada** (0 de 53 parcelas em PROD, 0 de 2 em DEV). **Decisão que não deve ser "corrigida": a trava é por policy, NÃO por gatilho.** A sugestão registrada em 2026-09-10 era criar um `BEFORE INSERT`; foi descartada por decisão do usuário ao se descobrir que `extra_payment_requests_insert` já resolve o mesmo problema pela policy — um gatilho criaria um segundo padrão para o mesmo problema no mesmo módulo. As duas abordagens são equivalentes inclusive quanto ao reparo manual: `service_role` não passa por RLS, como o escape hatch `auth.uid() IS NULL` não alcança o SQL Editor. Nenhuma tela mudou — o frontend já enviava `pendente_aprovacao` nos dois lotes, e agora dois testes unitários travam esse contrato. **A brecha foi reproduzida ao vivo antes da correção**: o E2E rodado pré-migration falhou no caso 01 porque o INSERT escalado passou; pós-migration, os 4 casos passam, incluindo o controle negativo que garante que a parcela legítima continua sendo criada. Verificação estrutural: `supabase/diagnostics/check-payment-installment-insert-status.sql` — **este roda em PROD**, ao contrário do E2E, que cria massa sintética e só roda em DEV. Portão: tsc 0, lint 0 erros/264 warnings, unitários 2.409/2.409 em 258 arquivos (+2 sobre o baseline de 2.407), smoke 7/7. Etapas 1, 2, 4 e 5 escritas pelo agente planejador; a 3 (E2E) foi estreia do `muse-spark-1.3-contributor-free`, com 2 correções do revisor — registro #047 do track record.

- **Protocolo de escolha de executor — Artificial Analysis como fonte única e piso de inteligência por grau (2026-09-11; documentação e script, nenhuma linha de `src/` tocada)**: a decisão de qual agente executa cada etapa passou a ter **duas camadas**: (1) `docs/EXECUTOR-TRACK-RECORD.md`, o histórico real, preferindo modelo **gratuito** e, na falta dele, o **mais barato entre os já assinados**; (2) o **Artificial Analysis Intelligence Index (AAII)** como **único** benchmark, aposentando SWE-Bench, GPQA, HLE, Vellum e LMSYS. Todo plano passa a declarar, por etapa, o **grau** (D1·D2·S1·S2·S3·N) e o **piso de AAII** exigido — **inclusive quando a escolha vem do histórico**, caso em que o piso é a exigência da tarefa e os registros são a justificativa (regra 16 dos prompts). **Pisos: 23 / 30 / 35 / 38 / 45**, ancorados em **modelos de referência nomeados**, não em números fixos nem em fração da fronteira. **Dois achados que mudaram o desenho no meio da execução:** (a) o AA **reescalou o índice** entre 22/08 e 11/09, com quedas de 12 a 17 pontos que **não** preservaram a ordem relativa — Opus 5 63→51, Sonnet 5 55→38, Nemotron 3 Ultra 38→23 —, o que falsificou a ancoragem por fração que o plano previa e obrigou a trocar pelo método de modelo de referência (S2 = Claude Sonnet 5, que é a barra `AA ≥ 55` antiga relida na escala nova); (b) o `EXECUTORS.md` afirmava **31 modelos gratuitos** e só existem **7** — o `~/.cache/opencode/models.json` estava defasado e `opencode models` é a fonte de verdade sobre existência. **Decisões que não devem ser "corrigidas":** (1) os registros do track record **não** foram reescritos — notas antigas são fato histórico e converter para a escala nova corromperia o arquivo; (2) `muse-spark-1.2-contributor-free` **saiu do índice** e a `1.3` entrou com AAII 48, mas a 1.2 continua sendo a escolha em combinação com 4+ registros, porque a camada 1 vence; (3) o preço voltou como **desempate** (gratuito → mais barato entre os assinados), nunca como orçamento; (4) a regra 15 foi **mantida** — nome de modelo continua fora do `IMPLEMENTATION.md`; piso é número e convive com ela. Novo `scripts/check-executor-docs.mjs` (`node`, fora do vitest): acusa termo aposentado, piso fora do conjunto oficial e cabeçalho de cache inválido; `exit 0/2`, com `--self-test` de 10 casos. Portão idêntico ao baseline: tsc 0, lint 0/264, unitários 2.407/2.407 em 258 arquivos, smoke 7/7. Etapa 7 delegada a executor de custo zero absoluto; as demais escritas pelo agente pelo teste de economia. Registro #045 do track record.

- **Templates de Checklist — visualização da estrutura publicada (2026-09-11; código implementado e portão automatizado aprovado; E2E pendente bloqueado por fixture preexistente)**: a coluna Ações de `/checklist-templates` mostra `Visualizar` somente para templates `published`, sem depender de permissão de escrita ou de cliente selecionado, e abre `ChecklistTemplateDetailModal` em modo somente leitura. O modal consulta `checklist_items` pela combinação `template_id` + `current_version`, preserva `order_number` e exibe metadados, flags reais, ação sugerida, loading, vazio, erro genérico e fechamento por botão/Escape. Nenhuma migration, RLS, dependência ou contrato de backend foi alterado. Foram adicionados 14 testes unitários/de integração; portão final: tsc 0, lint 0/264, unitários 2.398/2.398 em 257 arquivos e smoke 7/7. O E2E pendente não chegou ao novo caso porque a suíte serial falhou antes na expectativa preexistente de `Item obrigatório do sistema`; referências históricas a `Livre` continuam fora do escopo.
- **Checklists — busca por placa para Auditor e Gestor de Operações (2026-09-11; código implementado localmente, sem migration/RLS)**: `Yard Auditor` e `Operations Manager` agora filtram localmente, somente dentro da lista autorizada pelo contexto/escopo, o combobox `Veículo para vistoria` por placa completa ou trecho; o histórico desses papéis aceita placa, template e contexto, enquanto o fluxo do `Driver` permanece limitado a template/contexto. A implementação usa `src/lib/checklistSearch.ts`, integra `src/pages/Checklists.tsx` e atualiza quatro specs E2E. Portão final: tsc 0, lint 0/264 avisos, unitários 2.407/2.407 em 258 arquivos e smoke 7/7; E2E concluído do Auditor 2/2 e validação DEV do Gestor confirmou 7 opções autorizadas reduzidas a 1 pela placa digitada. O E2E completo do Gestor ficou parcialmente bloqueado por checklist em andamento preexistente; fixtures de Carlos/Handover continuam pendentes. O arquivamento permanece deliberadamente para outra sessão.
- **Financeiro — cancelamento de pagamentos aprovados pelo próprio aprovador, sem exclusão (2026-09-11; migration `20260911000000` aplicada em DEV e PROD pelo usuário, diagnóstico conforme nos dois bancos, **validado em tela em DEV pelo usuário (13 passos) em 2026-09-11**; **em produção** — commits `4276bba` (docs + migration) e `84c11d8` (código) em `main`)**: quem aprovou um pagamento, ou o Admin Master, pode cancelá-lo enquanto estiver `aprovado`, com motivo obrigatório (até 500 caracteres) e sem apagar o registro. Em Pagamentos Extras o cancelamento vale para o pedido inteiro e as parcelas acompanham; é recusado se alguma parcela já foi `pago`. Em Pagamentos é parcela a parcela, só origem OS, e a cancelada devolve o saldo. A ação fica no rodapé do detalhe (`CancelPaymentModal`). A fronteira real é o banco: gatilhos Fail Closed (`auth.uid()` NULL nunca é "o aprovador"), `cancelled_by/at` gravados pelo gatilho, `FOR UPDATE` contra a corrida com o Financeiro. **Decisões que não devem ser "corrigidas":** (1) o Admin Master cancela qualquer aprovado; (2) parcela de origem extra na aba Pagamentos não tem botão, só a dica; (3) o XLSX nunca inclui cancelada, mas continua incluindo reprovada; (4) o Financeiro vê extra cancelado só se chegou a ser aprovado (`approved_at IS NOT NULL`); (5) cancelado é terminal. Diagnósticos: `supabase/diagnostics/check-payment-cancellation.sql` (DEV/PROD) e `check-payment-cancellation-behavior.sql` (só DEV, 15 casos). Portão: tsc 0, lint 0/264, unitários 2.384/2.384 em 255 arquivos, smoke 7/7. Narrativa, achados e execução: `docs/MEMORY-HISTORY.md` → "Sessão — 2026-09-11: Financeiro — cancelamento…"; registros #034–#041 do track record.
- **Pagamentos — lançamento de parcelas a partir da aprovação do orçamento (2026-09-11; código pronto e verificado pelo portão, migration `20260910000000` **aplicada em DEV** pelo usuário e diagnóstico estrutural conforme nas 10 colunas em 2026-09-11, **validado em tela em DEV pelo usuário (9 passos) em 2026-09-11**, migration aplicada em PROD pelo usuário e diagnóstico estrutural conforme nas 10 colunas em PROD na mesma data; **em produção** — publicado em `main`)**: uma OS de manutenção passa a aparecer em Financeiro → Pagamentos → Cadastrar Pagamento assim que o orçamento é aprovado, porque as oficinas enviam NF e boleto nesse momento. Janela nova: `Orçamento aprovado`, `Serviço em execução`, `Concluído`, `Veículo retirado` (antes só os dois últimos); `Cancelado` e os status anteriores à aprovação continuam barrados. **Duas camadas mudaram juntas:** `PAYABLE_MAINTENANCE_STATUSES` (frontend) e `fn_enforce_payment_installment_source_payable` (banco) — mexer só no frontend faria o banco recusar o salvar. Como o pagamento pode ser lançado antes do serviço terminar, o status operacional da OS passou a ser exibido: no card de Aprovações (`Status da OS: …`) e na coluna Status do ledger de Pagamentos, com pílulas **contornadas** vermelha "OS cancelada" e âmbar "Serviço não concluído" (`src/lib/maintenanceOrderPaymentSignal.ts`). Cancelar uma OS com parcelas não reprovadas mostra aviso com quantidade e valor, e o botão vira "Cancelar mesmo assim" — **avisa, não bloqueia** (decisão do usuário); o botão fica desabilitado enquanto a verificação carrega (Fail Closed). Nada muda em permissões, no teto de valor (`approved_cost` congelado + `fn_enforce_payment_installment_budget_cap`), na máquina de estados, no modal de cadastro nem no XLSX. **Decisões que não devem ser "corrigidas":** (1) as pílulas da OS são contornadas porque o vermelho/âmbar sólido na mesma coluna já significa "Reprovado"/"Pendente de aprovação" da parcela; (2) esta sessão desfaz conscientemente a regra "só paga após conclusão/retirada" de 2026-08-26 (commit `b1e74a0`), por decisão do usuário. Diagnóstico estrutural: `supabase/diagnostics/check-payment-installment-payable-window.sql` — verifica estrutura, **não** comportamento: o escape hatch `auth.uid() IS NULL` faz todo INSERT pelo SQL Editor passar. Comportamento só se valida em tela (9 passos em `IMPLEMENTATION.md`, seção "Validação manual obrigatória em DEV"). Portão: tsc 0, lint 0 erros/264 warnings (desvio aceito, ver Decisões Vigentes), unitários 2.284/2.284 em 251 arquivos (+34 testes, +2 arquivos sobre o baseline de 2.250/249), smoke 7/7. Etapas 1–5 e 9 escritas pelo agente planejador; 6, 7 e 8 por `muse-spark-1.2-contributor-free` (custo zero absoluto), todas aprovadas na primeira execução e com controle negativo — registros #031–#033 do track record.
- **Financeiro → Pagamentos e Pagamentos Extras — rótulo de status "Pago" renomeado para "Lançado no sistema" (2026-09-11; validado em DEV — código não versionado ainda, aguardando commit do usuário)**: nas duas abas de Financeiro que compartilham o status `'pago'`, todo texto exibido mudou de "Pago" para "Lançado no sistema" — badge de status na tabela, opção de filtro, rótulo no modal de detalhe, botão de ação em massa ("Marcar selecionadas como Lançado no sistema"), aviso abaixo do botão, card de contagem ("Lançados no sistema") e os campos de auditoria "Pago por"/"Pago em" → "Lançado por"/"Lançado em". **O valor interno do status no banco (`'pago'`) não mudou** — é troca de texto de apresentação, sem migration, sem mudança de RLS, sem mudança de contrato. Criado `src/lib/paymentStatusDisplay.ts` (com `paymentStatusDisplay.test.ts`), centralizando `PAYMENT_INSTALLMENT_STATUS_LABELS` e `EXTRA_PAYMENT_STATUS_LABELS`, que antes estavam duplicados de forma idêntica em 4 componentes (`PaymentsTab.tsx`, `PaymentInstallmentViewModal.tsx`, `ExtraPaymentsTab.tsx`, `ExtraPaymentViewModal.tsx`) — segue o mesmo padrão já estabelecido em `workshopScheduleDisplay.ts`. **Decisão que não deve ser "corrigida": o rótulo "Pagamento" em `PaymentInstallmentViewModal.tsx` (linha ~163, mostra a data) é diferente de "Pago em" e foi deliberadamente preservado sem alteração** — só existe "Pago em"/"Lançado em" em `ExtraPaymentViewModal.tsx`. `STATUS_BADGE` (cores), `CATEGORY_LABELS`/`CATEGORY_OPTIONS` e `SOURCE_LABELS`/`SOURCE_OPTIONS` não foram tocados. As 2 specs pendentes que afirmavam o texto antigo (`e2e/pending/financeiro-payment-flow.spec.ts`, `e2e/pending/extra-payments-flow.spec.ts`) foram atualizadas por transcrição literal, mas sua **execução real continua bloqueada** por falta de `TEST_FINANCEIRO_EMAIL`/`PASSWORD` — a verificação desta sessão foi checagem visual manual real na tela (Playwright ad-hoc, Admin Master, `http://localhost:3000/financeiro`), que confirmou "Lançado no sistema" no filtro das duas abas. Portão: typecheck 0, lint 0 erros/263 warnings (patamar preservado), unitários 2.250/2.250 em 249 arquivos (+2 testes, +1 arquivo), smoke 7/7. Executado em 3 etapas por dois executores de **custo zero absoluto** (`big-pickle` e `muse-spark-1.2-contributor-free`), todas aprovadas sem correção do revisor. Registros #028 a #030 do track record.
- **Agendamentos — ações da linha migradas para o modal, padrão Master–Detail (2026-09-09; validado em DEV pelo portão automatizado, **pendente de validação em tela pelo usuário**)**: a coluna "Ações" de `/agendamentos`, que exibia de 3 a 5 ícones por linha conforme status e papel, passou a exibir **um único ícone** (o olho); a linha inteira também abre o modal. Editar, Concluir, Cancelar, Gerar OS e Excluir migraram para o rodapé do `ScheduleDetailModal` como botões **com rótulo em texto**. **Zero mudança de backend e zero mudança de permissão:** as cinco condições de disponibilidade foram transcritas sem alteração de valor para `src/lib/workshopScheduleActions.ts` (`isScheduleActionAvailable`), que virou a fonte única da regra — a fronteira real continua sendo o RLS `ws_schedules_*`. **Correção de segurança embutida:** Concluir, Cancelar e Excluir disparavam a mutation direto no `onClick`, **sem nenhuma confirmação** e de forma irreversível pela UI; agora exigem `window.confirm` com placa e oficina citadas (`buildScheduleConfirmMessage`), padrão já vigente em 11 pontos do projeto. Duas fontes secundárias de poluição saíram da linha: o link "Ver endereço" (redundante — o endereço por extenso e o link do Maps vivem no modal desde o commit `91de9db`) e o texto "Último Km: sem leitura" (via prop aditiva `hideWhenEmpty` em `LastKmLabel`, default `false`, sem efeito nas outras 4 telas que usam o componente). **Decisões que não devem ser "corrigidas":** (1) o rodapé usa `window.confirm`, NÃO `ConfirmDeleteModal` — confirmação por digitação é reservada a Veículos e Motoristas; (2) a confirmação vive na página, não no modal, que segue apresentacional; (3) o `<tr>` clicável NÃO tem `role`/`tabIndex` — o caminho acessível por teclado é o botão de olho; (4) menu suspenso "⋯" na linha foi avaliado e **descartado** nesta sessão por exigir um componente que o projeto não tem. `WorkshopSchedules.tsx` encolheu 40 linhas (+50/-90). Portão: typecheck 0, lint 0 erros/263 warnings (patamar preservado), unitários 2.248/2.248 em 248 arquivos (+26 testes, +3 arquivos), smoke 7/7. Executado em 6 etapas por dois executores de **custo zero absoluto** (`big-pickle` e `muse-spark-1.2-contributor-free`), com dois controles negativos executados pelo revisor. Registros #022 a #027 do track record.
- **Agendamentos — visualização somente-leitura para todos os papéis (2026-09-09; **em produção** — commit `91de9db` em `main`, validado em tela pelo usuário)**: a tela `/agendamentos` ganhou um ícone de olho na coluna Ações, **incondicional** — não depende de papel, permissão de escrita nem de cliente selecionado —, que abre `src/components/ScheduleDetailModal.tsx`, um modal apresentacional puro exibindo veículo, data, status, criador, nome da oficina, **endereço completo escrito por extenso**, link para o Google Maps e as **observações do agendamento** escritas pelo time de frota. O mesmo modal está disponível no cartão da visão do Motorista. **Causa raiz resolvida:** o endereço não era legível dentro do sistema (só existia como link externo) e o Gestor de Operações, sem permissão de edição, não tinha caminho nenhum para ler as observações — por isso o time de frota passou a colar o endereço à mão dentro do campo Observações. **Zero mudança de backend:** nenhuma migration, RLS, Edge Function, consulta ao Supabase ou campo de tipo foi tocado; todos os dados já chegavam à tela. Nenhuma permissão de escrita foi afrouxada. Constantes de rótulo/badge e formatação de data saíram de `WorkshopSchedules.tsx` para `src/lib/workshopScheduleDisplay.ts`, sem alteração de valor. **Decisão que não deve ser "corrigida": o campo exibido é `workshop_schedules.notes` (time de frota), NÃO `workshops.notes` (cadastro da oficina)** — este último foi explicitamente excluído do escopo pelo usuário. Portão: typecheck 0, lint 0 erros/263 warnings (patamar preservado), unitários 2.222/2.222 em 245 arquivos (+27 testes, +4 arquivos), smoke 7/7. Executado em 4 etapas por dois executores de **custo zero absoluto** (`big-pickle` e `muse-spark-1.2-contributor-free`), sem consumir janela de assinatura nenhuma — ver `docs/EXECUTOR-TRACK-RECORD.md` (registros #018-#021).
- Módulos estabilizados em produção: Cadastros, Manutenção, Checklists (offline-first), Pneus, Revisões de Garantia, Oficinas, Controle de Carretas, Dashboard Executivo, Self-service de senha, Financeiro.
- Fonte única de KM efetivo: funções `SECURITY DEFINER` (`get_vehicle_odometer_readings*`, `get_vehicle_odometer_summary`) com fallback para `vehicles.initial_km`. Não usar `checklists.odometer_km` para cálculos agregados.
- Papéis ativos: Admin Master (cross-tenant, `client_id = NULL`), Director, Manager, Coordinator, Supervisor, Fleet Analyst, Fleet Assistant, Driver, Yard Auditor, Operations Manager, Coupling Agent, Workshop, Financeiro.
- Bancos Supabase **separados** por ambiente: Dev `vvbnbzzhpiksacqudmfu`, Prod `oajfjdadcicgoxrfrnny`. Migrations validadas em Dev antes de Prod, só com autorização expressa.
- Performance: code splitting ativo; `npm run perf` mede regressão (tolerância 15%). Regressões aceitas (`route.veiculos.entryMs`, `route.pneus.requestCount`, `returnBehavior.returnEntryMs`) seguem como oportunidade futura.

## Pendências

- **Cancelamento de pagamentos — E2E do Financeiro (2026-09-11)**: publicado em produção (`4276bba` + `84c11d8`). Resta só o cenário 04 de `e2e/pending/payment-cancellation-flow.spec.ts` (o Financeiro vê o pedido e a parcela `Cancelado`), bloqueado por falta de `TEST_FINANCEIRO_EMAIL`/`PASSWORD`. O passo 7 da validação manual cobriu esse comportamento em tela.
- **Pagamentos a partir da aprovação — aplicar e validar (2026-09-11)**: (1) ~~usuário aplica a migration em DEV~~ feito; (2) ~~diagnóstico estrutural em DEV~~ feito — gatilho ativo (`O`) e as 10 colunas conforme; (3) ~~validação em tela, 9 passos~~ feito e confirmado pelo usuário em 2026-09-11; (4) ~~migration em PROD + diagnóstico~~ feito em 2026-09-11, antes do push do front. Resta apenas o E2E abaixo. Se o passo 3 da validação falhar com `Pagamento de manutencao exige...`, a migration não está no banco que o front usa. O E2E `e2e/pending/maintenance-budget-status-coherence.spec.ts` foi atualizado (agora usa o combobox do `SearchableSelect` — as assertivas antigas sobre `<select>` estavam defasadas desde 2026-09-01) mas **não rodou**: exige ambiente DEV e credenciais `TEST_MANAGER_*`. Reexecução, depois da migration em DEV: `PLAYWRIGHT_INCLUDE_PENDING=1 npx playwright test e2e/pending/maintenance-budget-status-coherence.spec.ts --project=chromium`. Obs.: o `tsconfig.json` só inclui `src/`, então `tsc` não verifica specs E2E; a única checagem feita foi o parse do Playwright (`--list`).
- **Execução real das specs E2E de Pagamentos após a renomeação de "Pago" para "Lançado no sistema" (2026-09-11)**: `e2e/pending/financeiro-payment-flow.spec.ts` e `e2e/pending/extra-payments-flow.spec.ts` foram atualizadas por transcrição literal, mas nunca rodaram via Playwright nesta sessão — faltam `TEST_FINANCEIRO_EMAIL`/`PASSWORD` (e `TEST_WORKSHOP_EMAIL`/`PASSWORD` para o cenário 08 da segunda suíte). A verificação feita foi visual manual (Admin Master, tela real). Executar quando o ambiente DEV tiver essas credenciais.
- **Limpeza dos endereços colados à mão em `workshop_schedules.notes` (2026-09-09; contexto atualizado na mesma data)**: com o link "Ver endereço" fora da linha, esses registros agora exibem o endereço apenas dentro do modal — mas ainda duplicado, uma vez no campo Endereço e outra dentro das Observações. Vários agendamentos têm o endereço da oficina digitado dentro das observações — a gambiarra que o modal torna desnecessária. Agora esses registros exibem o endereço duas vezes. É correção de dado em produção, `Não delegável`, e exige decisão do usuário sobre o critério de identificação e sobre preservar o texto útil misturado ao endereço (um "Procurar o mecânico Tião" não pode ser apagado junto). Sessão própria.
- **Sem backfill, `profiles.active` representa mal quem já estava inativado antes da migration (2026-09-08)**: a migration `20260908000000` cria `active` com `DEFAULT true` e o plano **proíbe backfill** — decisão defensável, mas a consequência é que quem foi inativado pelo caminho antigo fica com `drivers.active = false` e `profiles.active = true`. As telas Usuários e Motoristas passam a discordar sobre essas pessoas. Em DEV é **1 caso** (Pedro Paulo, perfil `37267365-6b75-4023-b227-32d84690f99e`, motorista inativado em 2026-07-02 e banido no Auth até 2036 — é também o único desvio da consulta de coerência, ou seja, os dois achados são a mesma pessoa). **A magnitude em PROD não foi medida** (a consulta somente-leitura foi bloqueada pelo classificador de permissões). Medir antes de aplicar em PROD com:
  ```sql
  SELECT count(*) FROM public.drivers WHERE active IS NOT TRUE AND profile_id IS NOT NULL;
  ```
  Se o número for relevante, decidir explicitamente entre (a) aceitar a divergência, (b) fazer um backfill pontual `profiles.active = false` para esses perfis — o gatilho tem escape hatch por `auth.uid() IS NULL`, então o SQL Editor consegue —, ou (c) alinhar depois pela tela.
- **Critério de conclusão inatingível como escrito — consulta de coerência `profiles` × `auth.users` (2026-09-08)**: o plano exige **0 linhas**, mas DEV devolve **1** (`pedropaulo@betafleet.com.br`, banido até 2036 pela ação `block`, com `active = true`). Não é defeito das migrations: é a colisão `block`/`set_active` registrada em Observações. Consequência prática: o **cenário 8 do E2E da Etapa 10 vai falhar em DEV** enquanto existir usuário bloqueado por `block`. Antes de tratar como regressão, decidir se a consulta deve excluir esses casos ou se os dois mecanismos devem ser unificados.
- **`vehicles.status` é dado morto em todo o projeto**: 334 de 336 veículos em `'Available'`, 2 em `'Maintenance'`, nenhum em `'In Use'` (verificado em 2026-09-07). A coluna existe no schema e no tipo `Vehicle`, mas nada a mantém — descoberto ao planejar Utilização MELI, cuja coluna Status usa `maintenance_orders` em vez desse campo por esse motivo. Ou o campo passa a ser alimentado (gatilho a partir de `maintenance_orders`), ou deveria ser removido para não induzir outro módulo ao mesmo erro. Sessão própria.
- **Abastecimento — integração real com a Veloe pendente das credenciais (atualizado em 2026-09-06)**: tabela, RLS, Edge Function e frontend já estão publicados em **DEV e PROD**, e a tela `/abastecimento` já está visível em produção para a Deluna (gate de tenant validado por inspeção do bundle). Falta apenas a parte que depende do representante Veloe: as 4 credenciais da API, os secrets da função, a migration do `pg_cron` e a carga histórica. Checklist completo e ordenado, com todos os UUIDs/refs de DEV e PROD e as armadilhas já pisadas (env var do Vercel, Vite queimando o valor no build, etc.): **`docs/PENDENCIAS-ABASTECIMENTO-VELOE.md`** — consultar esse arquivo antes de continuar este trabalho.
- **E2E de Chamados (`e2e/pending/fleet-tickets-flow.spec.ts`, cenário 3) — validação bloqueada (2026-09-06)**: o arquivo foi atualizado nesta sessão para percorrer os dois avisos (com asserção intermediária `Guia de classificação` contra flakiness), mas vive em `e2e/pending` e exige servidor, credenciais e o usuário Yard Auditor de teste (`e2e/.auth/carlos.json`). Executar quando o ambiente DEV estiver autenticado: `PLAYWRIGHT_INCLUDE_PENDING=1 npx playwright test e2e/pending/fleet-tickets-flow.spec.ts --project=chromium`. Enquanto isso, o fluxo `botão → dois avisos → modal` está coberto pelos 3 testes de integração de página da suíte padrão.
- **Validação visual em dispositivo móvel do fluxo de dois avisos**: abrir `/chamados` em viewport móvel e clicar em "Novo chamado" — o card usa `max-w-lg` com `p-4` no overlay, padrão já validado nos demais modais, risco baixo.
- **Fase 4 da auditoria — rastro de exclusão** — nova fase dedicada. Escopo: estender o padrão das Fases 1, 2 e 3 para capturar eventos `'deleted'` de **todas** as 11 entidades auditadas de uma vez. Mudança de contrato: tornar `<entidade>_id` nullable com `ON DELETE SET NULL` para o evento sobreviver ao pai; os `'deleted'` vão para o evento com `*_id = NULL` e `client_id` preservado. Abre a possibilidade de responder "quem apagou X?".
- **Validação manual da reabertura de orçamento**: os 10 passos da Etapa 8.2 de `IMPLEMENTATION.md`, incluindo o passo 10 (conta de oficina não vê a ação e o reenvio por PDF continua funcionando).
- **Rodar `supabase/diagnostics/check-approved-budget-integrity.sql` em DEV e PROD**: mede quantas OS aprovadas já estão com a lista de itens divergindo de `approved_cost` (a OS-2606-8910 é uma delas, com R$ 5.000 a mais). O arquivo traz o modelo de reparo comentado — Caminho A (remover o item acrescentado após a aprovação, conferindo contra o PDF) ou Caminho B (devolver a OS para a fila de aprovação para que o novo valor tenha decisão registrada). Nunca ajustar `approved_cost` para bater com os itens.
- **Validação manual das duas travas**: os 7 passos de `IMPLEMENTATION_FIXBUG.md` (oficina) mais o caso do cliente — abrir "Editar OS / Orçamento" de uma OS aprovada como Fleet Assistant+ e confirmar tabela em leitura, sem upload de PDF, com os demais campos ainda editáveis e salvando sem erro.
- **Confirmação factual em PROD do episódio RKR4C49/RJF3B62**: rodar as 3 consultas registradas em `IMPLEMENTATION_FIXBUG.md` (seção "Confirmação factual pendente em PROD") para confirmar a sequência exata do checklist gravado na placa errada. O reparo de dados (excluir o checklist errado e refazer a inspeção) já tem roteiro pronto na mesma seção, mas depende dessa confirmação primeiro.
- **Ajustar sequenciamento de `checklist-offline-fill.spec.ts` (teste B.1) e `checklist-offline-reload.spec.ts`**: falha pré-existente, não introduzida pela correção de 2026-08-21 — o teste A.1 deixa propositalmente um checklist em andamento sem limpeza, e os dois specs seguintes assumem poder iniciar um checklist novo em seguida, o que o bloqueio `disabled={!!openChecklist}` (já existia antes desta correção) impede. Precisam finalizar/cancelar o checklist de A.1 antes de iniciar um novo.
- **Validação manual da entrega de Checklists de 2026-08-20**: conferir percentuais nos dois níveis do drill-down, Dashboard sem sub-rótulos, embarcador/unidade na coluna Veículo e as duas abas do XLSX, inclusive filtro "Com inconformidades" e ausência de links de CNH, assinatura e hodômetro.
- **Fechar o bucket `checklist-photos` — prioridade alta, sessão dedicada**: o bucket público contém fotos operacionais, CNH e assinaturas. A exportação nova consulta somente fotos de respostas com inconformidade, mas o fechamento do bucket, a migração dos links e a adoção de URLs assinadas permanecem fora desta sessão.
- **Validação manual da sub-aba Aderência**: executar os 12 passos já desbloqueados; `auditoria_day_interval` e o contexto `Auditoria` da RPC foram confirmados em DEV e PROD.
- **E2E de placas do Gestor de Operações**: a comparação explícita permanece bloqueada até configurar `TEST_GESTOROP_EXPECTED_VEHICLE_PLATES`; os outros três cenários do fluxo novo foram validados em DEV e os testes em PROD foram aprovados pelo usuário.
- **OCR Gemini V-06**: confirmar os limites server-side da Edge Function `gemini-ocr`; essa configuração não foi verificável por leitura nesta sessão.
- **Validação manual de Plano de Ação a partir de Chamado**: executar os 14 passos já desbloqueados; `20260807020000_action_plan_from_fleet_ticket.sql` foi confirmada em DEV e PROD.
- **Validação manual do carimbo e tratamento por chamado**: executar os 12 passos funcionais; a migration e seus 11 objetos estruturais já foram confirmados em PROD.
- **Confirmação estrutural por SQL nos dois bancos**: `trg_enforce_budget_discount_exclusivity_item` e `trg_enforce_budget_discount_exclusivity_order` foram vistos em DEV em 2026-08-22 (saída da consulta da Etapa 0). Falta confirmar `action_plans_origin_check` e `trg_enforce_payment_installment_budget_cap`, e repetir tudo em PROD com a consulta da Etapa 8, passo 0, de `IMPLEMENTATION.md`. As colunas `budget_discount`, `maintenance_budget_items.discount`, `payment_installments.invoice_number`, `nota_fiscal_url_2` e `maintenance_orders.budget_rejection_reason` já existem em PROD; `budget_rejection_reason` também existe em DEV.
- **Recadastrar Yard Auditor de teste** para `e2e/pending/handover-checklist.spec.ts`; atualizar `.env.local` e mover specs para `completed/`. (A conta de Driver de teste — Jorge — já foi corrigida em 2026-08-21: registro em `drivers` criado, veículo KM035012 atribuído, checklist órfão removido.)

## Próximos Passos

- Investigar regressões de performance aceitas.
- Migração incremental de páginas menores para React Query.
- Acessibilidade: violações de `color-contrast`/`select-name` (relatório em `.claude/reports/a11y-core-screens-report.md`).
- Unificar estilo de RLS entre DEV e PROD (helpers vs subqueries inline).
- SMTP customizado + templates de Auth PT-BR.
- Corrigir `scripts/apply-migration.mjs` (split por `;` e `$$...$$`).
- Investigar 4 specs E2E de Revisão de Garantia falhando pré-existentemente.
- RPC fantasma `get_vehicle_last_odometer_reading_at` — criar migration e remover exceção.
- Endurecer `escapeCsv` contra CSV formula injection.
- [Oportunidades Futuras](docs/OPORTUNIDADES_FUTURAS.md).

## Decisões Vigentes

- **O piso de AAII é declarado em todo plano, mesmo quando a escolha do executor veio do histórico (2026-09-11)**: piso e justificativa são coisas distintas. O **piso** é a exigência da tarefa, determinada pelo **grau** da etapa (D1 23 · D2 30 · S1 35 · S2 38 · S3 45 · N sem piso). A **justificativa** é o que sustenta aquela escolha — que pode ser o histórico do `EXECUTOR-TRACK-RECORD.md`, inclusive para modelo **sem nota pública** como o `big-pickle`. Nesse caso o plano declara o piso, diz explicitamente que o modelo não é catalogado, e cita os registros. Regra 16 de `prompts/Evolucao.md`. Os pisos são ancorados em **modelos de referência nomeados** e relidos a cada atualização de `docs/model-cache.md`; se uma âncora sumir do índice, o piso **congela** e a troca exige decisão do usuário.

- **RISCO ACEITO (2026-09-11) — estorno de valor já pago fora do sistema**: cancelar um pagamento no βetaFleet não estorna nada no banco. Se o valor já foi transferido, o estorno é tratado fora do sistema. A janela de cancelamento avisa isso em texto fixo (`CANCELLATION_IRREVERSIBLE_WARNING`). Fora do escopo por decisão do usuário.
- **RISCO ACEITO (2026-09-11) — aprovador rebaixado ou inativado**: a trava compara `approved_by`/`payment_approved_by` com `auth.uid()`. Se o aprovador perder o papel ou for inativado, só o Admin Master consegue cancelar aquele pagamento. Decisão do usuário: o Admin Master é a saída.
- **RISCO ACEITO (2026-09-11) — porta de INSERT do papel `Workshop` em `payment_installments`**: a policy `payment_installments_insert` aceita `Workshop` para OS da própria oficina desde 2026-07-08. A interface não expõe criação à oficina (`ROLES_CAN_CREATE_PAYMENTS` não inclui o papel, embora `ROLES_CAN_VIEW_PAYMENTS` inclua — a oficina vê a aba). Com a janela pagável ampliada, um INSERT direto por API passa a ser aceito desde a aprovação do orçamento, não mais só depois da conclusão. Continuam barrando: teto de valor, integridade de tenant, baixa exclusiva de `Financeiro`/`Admin Master` e o evento `created` em `payment_installment_events`. **Atualizado em 2026-09-12:** a pendência irmã, do `status` no INSERT, foi fechada pela migration `20260912000000` — a oficina que inserir por API já não consegue escolher o status nem carimbar auditoria, a parcela nasce obrigatoriamente `pendente_aprovacao` e passa pelo Coordenador. **O que permanece aberto é só a porta em si**: a oficina ainda pode criar uma parcela pendente numa OS sua, por API, sem que a interface ofereça isso. Fechar a porta é sessão própria.
- **DESVIO ACEITO (2026-09-11) — lint em 264 warnings, +1 sobre o baseline**: `src/pages/Maintenance.tsx` tem `return <Navigate />` antes dos hooks (mesmo defeito de `Users.tsx`), que já gerava 23 warnings `react-hooks/rules-of-hooks`. O `useQuery` do aviso de cancelamento precisa de `orderToCancel`, declarado depois desse `return`, e gera o 24º. Segue o precedente de 2026-09-08: o warning é sinal verdadeiro de defeito e não deve ser suprimido. A correção do componente é sessão própria.
- **Modelo de execução — orquestração centralizada (2026-09-08)**: o usuário **não executa código, não dispara agentes e não roda comandos no terminal**. O agente que conduz a sessão acumula os três papéis — planejador, orquestrador e revisor —, dispara todos os executores via Bash na ordem definida pelo plano, lê os logs, reexecuta as verificações contra o baseline e confere o `git status` contra o manifesto. Antes de começar, apresenta o roster (qual executor roda cada etapa, em que ordem e por quê) e aguarda um comando único de início; a partir daí executa a sequência inteira sem devolver o terminal. Regra canônica em `agent/AGENT.md` §4, propagada para `docs/EXECUTORS.md`, `docs/MODEL_SELECTION.md` (Passo 6), `prompts/Evolucao.md` e `prompts/Fixbugs.md`. Três consequências: (a) a coluna "Quem dispara" vale `agente planejador` em todas as linhas de toda tabela de distribuição; (b) ferramenta sem modo não-interativo é inutilizável — a `freebuff` saiu do parque efetivo por isso; (c) o custo de orquestração deixou de ser opcional, e a economia migrou para fechar a especificação até o ponto em que um único ciclo baste. Continuam sendo decisões exclusivas do usuário: aplicar migration em DEV e PROD, publicar Edge Function e executar `git commit`.
- **Usuários — exclusão restrita ao Admin Master e inativação transacional (2026-09-08; **em produção e validado pelo usuário na tela** — commit `ef00efa` em `main`, migrations e Edge Functions aplicadas em DEV e PROD)**: a exclusão definitiva passou a ser exclusiva do `Admin Master`, fechada em três camadas (allowlist literal na UI, policy `tenant_managers_delete_profiles` removida, e trava em `create-user` e `delete-user`). A inativação/reativação espelha Veículos e Motoristas. **A operação de ativação é transacional** e vive na ação `set_active` de `create-user`, na ordem autorizar → Auth → banco, com compensação — a falha cai sempre no lado seguro ("aparece Ativo e não consegue entrar"), nunca no perigoso ("aparece Inativo e continua logando"). **Decisão que não deve ser "corrigida": a Edge Function escreve com o JWT do chamador, jamais com `service_role`** — com `service_role`, `auth.uid()` seria NULL, o escape hatch do gatilho dispararia e as quatro invariantes seriam puladas em silêncio, sem quebrar nenhum teste. As invariantes (não alterar o próprio status; piso `Coordinator`, exceto papel `Driver` cujo piso é `Fleet Analyst`; só rank estritamente inferior; nunca inativar o último `Admin Master` ativo) vivem em **uma** função SQL, `fn_profile_activation_denial_reason`, consultada pelo gatilho e pela Edge Function — não há cópia em TypeScript. `set_profile_activation` é `SECURITY INVOKER` e grava `profiles` e `drivers` em uma transação só. Portão: typecheck 0, lint 0 erros/263 warnings, unitários 2.195/2.195 em 241 arquivos, smoke 7/7. Narrativa completa, validações e achados: `docs/MEMORY-HISTORY.md` → "Sessão — 2026-09-08".
- **RISCO ACEITO (2026-09-08) — sessão em curso sobrevive à inativação**: o usuário inativado é barrado apenas no próximo login; o access token vigente continua válido até expirar (~1h). Decisão explícita do usuário. Não implementar derrubada imediata de sessão sem pedido novo.
- **DESVIO ACEITO (2026-09-08) — lint em 263 warnings, +1 sobre o baseline**: `src/pages/Users.tsx` chama `return <Navigate />` antes dos hooks, defeito pré-existente que já gerava 5 warnings de `react-hooks/rules-of-hooks`. A `toggleActiveMutation` nova adiciona o sexto. O usuário optou por **manter o warning** em vez de realocar o hook: ele é um sinal verdadeiro de que o componente precisa de correção, e suprimi-lo esconderia o problema. A correção do componente é sessão própria.
- **Correção do Passo 4.1 e higiene de contexto (2026-09-08, ao final da sessão)**: a regra "se o artefato está literal no plano, delegue" **estava errada** e foi medida como tal. Para delegar, o agente emite o artefato dentro de um prompt de executor — se ele já está escrito por inteiro no plano, isso é datilografar em dobro, mais o custo de ler o log, verificar e revisar. Vale agora o **teste de razão spec/artefato**: `spec ≈ artefato` → o agente escreve; `spec << artefato` (E2E a partir de cenários, componente a partir de contrato) → delegue. Risco e custo são eixos independentes e, para artefato literal, apontam em direções opostas; **quando divergirem, o custo decide**. Registrado também que higiene de contexto é responsabilidade do agente (editar em vez de reescrever, ler trecho em vez de arquivo inteiro, nunca emitir o mesmo artefato duas vezes), e que `/clear` e `/compact` são sugeridos apenas em **fronteira de fase**, nunca entre etapas — cada `/clear` custa reler o `IMPLEMENTATION.md`. Regras em `agent/AGENT.md` §4 e `docs/MODEL_SELECTION.md` Passo 4.1.
- **Máxima de delegação: qualidade com o melhor custo-benefício (2026-09-08)**: três regras acopladas, registradas em `docs/MODEL_SELECTION.md` (Passos 4.1 e 4.2) e propagadas para `prompts/Evolucao.md` e `prompts/Fixbugs.md`. **(a) `Não delegável` protege a decisão, não a digitação** — quando o plano já traz o artefato literal e completo, o executor não precisa abrir outro arquivo e existe verificação mecânica do resultado, a etapa é rebaixada para `Supervisionado / transcrição` com revisão linha a linha. Continuam `Não delegável` os casos de **merge** em código existente, de **armadilha de falha silenciosa** (erro que não quebra teste e desliga garantia de segurança) e de correção de dado em produção. **(b) Estrear executor sem histórico é comportamento desejado**, não risco a evitar: benchmark é indício, e o `EXECUTOR-TRACK-RECORD.md` só sai do zero se os modelos forem rodados. Preferir quem atende à barra (SWE-Bench alto ou AA ≥ 55); na falta, um pouco abaixo da barra é aceitável em etapa de risco baixo com revisão linha a linha — **nunca** em fronteira de tenant/auth/RLS ou migration com dado real. Uma estreia por plano, registro obrigatório com atribuição de falha, e regra de queima após duas falhas de modelo na mesma forma. **(c) Verificação mecânica por script, não por leitura de log** — ver `scripts/plan-runner.mjs` abaixo.
- **`scripts/plan-runner.mjs` — runner de execução de plano (2026-09-08, criado e validado)**: executa e verifica uma etapa de `IMPLEMENTATION.md` sem gastar tokens do agente orquestrador. Dispara o executor, destila o log (linhas suspeitas + cauda), compara `git status` com o manifesto da etapa, roda `tsc`/`lint`/`unit`/`smoke` e compara com o baseline, e emite veredito (`exit 0` passou, `2` reprovado). Config por sessão em `scripts/plan-runner.config.json`. **`node scripts/plan-runner.mjs snapshot` é obrigatório antes da primeira etapa** — este repositório carrega 112 arquivos sujos sem relação com o plano (medido em 2026-09-08) e, sem o snapshot prévio, a checagem de escopo acusa violação em tudo. É script e não modelo de propósito: `git status` e contagem de teste são determinísticos, e um modelo resumindo log reportaria sucesso no caso já documentado do codex devolvendo `exit 0` sem ter executado nada. **Portão verde não é aprovação** — é permissão para começar a revisão de código, que continua sendo do agente.
- **Consumo de janela de 5h como critério de escolha de executor (2026-09-08)**: `docs/MODEL_SELECTION.md` e `docs/EXECUTORS.md` tratavam "custo marginal zero" como se assinatura já paga fosse recurso infinito, e por isso recomendavam `gpt-5.6-sol` como default para `Supervisionado`. O usuário reportou as duas janelas (codex/ChatGPT e opencode pago) apertadas simultaneamente. Correções aplicadas: o consumo de janela virou eixo explícito do Passo 4; preço `$/M` passa a ser aceito como **proxy de velocidade de queima** (nunca como orçamento); `model_reasoning_effort` foi reconhecido como a alavanca principal, com `medium` virando padrão para `Supervisionado` com spec fechada; o comando canônico do `codex` passou a explicitar `-m gpt-5.6-luna` (AA 52, SWE-Bench 93,0%, output $1,20) em vez do Sol por omissão (AA 61, SWE-Bench 96,2%, output $20-30). Documentado também o procedimento de **mapeamento por gêmeo pago** para descobrir benchmark de modelo gratuito — comparar o preço do gêmeo pago com o `model-cache.md` e só aceitar se for idêntico, nunca estimar por família. Por esse método, `muse-spark-1.2-contributor-free` foi confirmado como **AA 57 — o único gratuito do parque que alcança o piso de aptidão de `Supervisionado`**. Corrigida ainda a tabela de gratuitos do `EXECUTORS.md`, que ordenava por contexto e destacava o `nemotron-3-ultra-free` (1M de contexto) sem registrar que ele tem **AA 38, o mais fraco dos catalogados**.
- **Tamanho de `docs/MEMORY.md` (2026-09-06)**: o critério "abaixo de 6.000 bytes" da Etapa 0 do `IMPLEMENTATION.md` do módulo de Abastecimento foi **dispensado pelo usuário**. A Etapa 0 arquivou os 12 registros antigos da seção "Estado Atual" em `MEMORY-HISTORY.md` (nada perdido), e o arquivo fica no tamanho atual (~31 KB). Razão: os 5 registros mais recentes que o próprio plano manda preservar somam ~7 KB, e Pendências / Próximos Passos / Decisões Vigentes / Protocolo de smoke somam ~18 KB — nenhuma dessas seções a Etapa 0 autorizava tocar. Não reabrir sem pedido explícito. Revisto em 2026-09-11 a pedido explícito do usuário: a seção Estado Atual foi arquivada; as demais seções permanecem fora do critério de tamanho.
- **A fricção de dois avisos antes de abrir chamado é intencional (2026-09-06).** O objetivo declarado pelo usuário é dificultar a abertura de chamados desnecessários ou duplicados. Dois passos separados, sempre nesta ordem, sem atalho; proibido qualquer mecanismo de "não mostrar novamente", persistência, deep link ou condicionamento a papel/horário. Um agente futuro que ler isto como má usabilidade deve **manter o comportamento**.
- **RISCO ACEITO — RLS de `vehicles` ampliada para `Financeiro` (2026-09-01).** A policy mantém a trava pelo `client_id` do perfil, portanto não cria acesso cross-tenant, mas permite ao papel ler todas as colunas de veículos do próprio tenant porque RLS é por linha, não por coluna. A escolha segue o precedente de `20260711000000_allow_financeiro_read_maintenance_workshop.sql` e o papel já lê ordens, orçamentos, notas fiscais e custos do mesmo tenant.
- **A placa de veículo não entra no XLSX de Pagamentos (2026-09-01).** A coluna é apenas de tela e filtro local; `paymentTemplateRows.ts` mantém byte a byte o contrato vigente de 10 colunas.
- **RISCO ACEITO — dados de contato de embarcador nos eventos de auditoria (2026-08-26).** `shipper_events.old_value`/`new_value` guardam `phone`, `email` e `contact_person` (nome de pessoa física). São dados pessoais, mas já visíveis a Fleet Assistant+ do mesmo tenant na tela de Embarcadores — a superfície de exposição não aumenta, e a policy da tabela de eventos é exatamente a mesma. Segue o precedente da validade de CNH em `driver_events` (2026-08-22). CNPJ, `notes` e todos os uploads permanecem proibidos no payload.
- **RISCO ACEITO — validade de CNH gravada nos eventos de auditoria (2026-08-22).** `driver_events.old_value`/`new_value` guardam a data de validade da CNH do motorista em eventos `cnh_validity_changed`. É dado pessoal, mas é uma data já visível a Fleet Assistant+ do mesmo tenant na tela de Motoristas — a superfície de exposição não aumenta, e a policy da tabela de eventos é exatamente a mesma. Segue o precedente do `actor_name_snapshot` e do nome do motorista na sub-aba Aderência. Nome, CPF, telefone e todos os uploads permanecem proibidos no payload.
- **RISCO ACEITO — nome do autor gravado nos eventos de auditoria (2026-08-22).** `actor_name_snapshot` guarda o nome de usuário interno de quem fez a escrita, visível a Fleet Assistant+ do mesmo tenant e ao Admin Master. É um dado pessoal, mantido porque o snapshot preserva a integridade do log mesmo depois de o perfil ser excluído (`actor_id` usa `ON DELETE SET NULL`). Segue o precedente já vigente para `budget_reviewed_by` e para o nome de quem reabriu orçamento. Minimização aplicada ao resto do payload: só identificadores, status e valores de negócio — texto livre, documentos, chaves PIX e URLs de anexo são proibidos.
- **Orçamento reprovado é reabrível; aprovado nunca.** Só a transição `reprovado → reaberto` é aceita, e sempre com justificativa obrigatória validada em três camadas (botão `disabled`, `throw` em `reopenRejectedBudget` e o CHECK `maintenance_budget_reviews_reason_required`). `aprovado` não é reabrível nem por Admin Master pela interface — reparo continua exigindo SQL Editor. Não há limite de reaberturas por OS. Notificação de reabertura para a oficina foi explicitamente descartada.
- **Livro-razão append-only de decisões de orçamento.** `maintenance_budget_reviews` não tem policy de `UPDATE` nem de `DELETE`, e o gatilho `trg_budget_reviews_append_only` recusa as duas operações. Nenhum código de aplicação pode alterar ou apagar linha do ledger; a reprovação anterior permanece visível depois de qualquer número de reaberturas. `fn_guard_budget_reopen` **não** tem escape hatch por `auth.uid() IS NULL`: reparo por SQL Editor deve passar por `reprovado` antes de `reaberto`, ou gravar direto o valor final desejado.
- **RISCO ACEITO — nome de quem reabriu exibido em tela (2026-08-22).** Dado pessoal de usuário interno, exibido na linha do tempo do Histórico de Orçamentos às mesmas funções que já veem o nome do aprovador. Segue o precedente já vigente para `budget_reviewed_by`.
- **Orçamento aprovado é imutável para todos os papéis**: com `budget_status = 'aprovado'`, itens, descontos, PDF e os custos estimado/aprovado ficam congelados para oficina, Fleet Assistant+, Financeiro e Admin Master. A oficina segue editando previsão de saída, OS da oficina, mecânico, km e fotos das peças, e pode iniciar o serviço a partir de `Orçamento aprovado`; o cliente segue editando todo o resto da OS (status, tipo, datas, descrição, notas). Reenvio de orçamento continua permitido enquanto `budget_status` for `pendente`, `reprovado` ou `sem_orcamento`. Consequência aceita: uma OS aprovada não volta mais para a fila de aprovação pela interface — serviço que cresceu depois da aprovação exige reabertura manual pelo SQL Editor (Caminho B do diagnóstico).
- **Escape hatch dos gatilhos de orçamento aprovado**: `auth.uid() IS NULL` (SQL Editor e `service_role`) libera a escrita. É deliberado — é o único caminho de reparo de dados — e não é alcançável pela aplicação, que sempre atua com JWT de usuário. Qualquer job novo que rode com `service_role` e escreva em `maintenance_orders`/`maintenance_budget_items` passa por fora da trava; avaliar caso a caso.
- **Admin Master** com `currentClient = null` = visão cross-tenant; RLS já permite leitura.
- **CRLV**: precedência data real de vencimento → ano (`crlv_year` é fallback).
- **Escopo temporal do Dashboard**: `Visão Geral`/`Operação`/`Conformidade` são situação atual; só `Custos` obedece ao período.
- **KM por veículo**: `MAX(effective_km) − MIN(effective_km)` sobre `vehicle_odometer_effective_readings`, com `HAVING COUNT >= 2 AND (MAX−MIN) > 0`.
- **Conformidade documental**: Itens Críticos = Documentos Vencidos + Ausentes.
- **Cache**: política central em `src/lib/cachePolicy.ts` (allowlist default-deny, TTL, `buster: 'v4'`).
- **Storage de documentos (V-01)**: buckets privados; banco guarda caminho, leitura gera URL assinada de 3600s. `checklist-photos` permanece público (fora do escopo).
- **OCR Gemini (V-06)**: autorizado com limites server-side (10 MB, MIME+assinatura, 20 chamadas/h, 100 MB/h).
- **Conformidade de checklist**: fonte única é a RPC `dashboard_last_checklist_per_vehicle` (`SECURITY INVOKER`), que desde 2026-08-19 devolve `Rotina`, `Segurança` e `Auditoria`. O agregado do Dashboard e o filtro de Veículos usam **apenas** Rotina ∪ Segurança; a sub-aba Aderência usa os contextos individualmente. Regra em `computeOverdueChecklistVehicleIdsByContext` (`src/lib/dashboardKpi.ts`).
- **Exportação de checklists**: um XLSX com as abas `Checklists` e `Inconformidades`, derivado da mesma lista filtrada da tela. Somente fotos de resposta com `status = 'issue'` entram; `cnh_photo_url`, `signature_url` e `odometer_photo_url` são proibidos.
- **Carimbo de plano de ação no checklist**: estado derivado, nunca persistido. Prioridade: tratado por chamado > em andamento > concluído > sem carimbo. Todos os planos `cancelled` equivalem a sem plano. Regra em `src/lib/checklistActionPlanStamp.ts`.
- **Sinalização "tratado por chamado"** vive em `checklist_ticket_treatments`, nunca em `checklists` — a policy `checklists_update` restringe a edição ao preenchedor com status `in_progress`, e abrir isso permitiria alterar checklist concluído.
- **Checklist concluído é imutável**: a tela de preenchimento recusa abrir checklist com status `completed` e a policy `responses_insert` exige `status = 'in_progress'`. O botão de finalizar fica bloqueado enquanto houver operações pendentes na fila offline — sem esse bloqueio, a policy descartaria respostas legítimas atrasadas em silêncio.
- **`openChecklist` não é cacheável**: a chave saiu da allowlist de persistência (`src/lib/cachePolicy.ts`) por ser um ponteiro de escrita (qual checklist o motorista vai preencher), não um dado de consulta. Custo aceito: quem abre o app já offline não retoma checklist pela tela inicial.
- **Gate `lastRoute` (Deluna)**: fonte única na UI é `src/hooks/useVehicleLastRoutes.ts`, consumido por Cadastros → Veículos e Manutenção. Manutenção exibe somente a data (`LastRouteLabel` variante `dateOnly`); Veículos exibe data + ID. A diferença é intencional, não inconsistência.
- **Permissão de UI espelhando RLS por allowlist literal**, não rank (ler o SQL da policy em `supabase/migrations/`).
- **Auth por ambiente**: Site URL/Redirect de PROD → `https://app.betafleet.com.br`; DEV → `http://localhost:3000`.
- **Convenção `bf:v1:ui`** para persistência de estado de UI.
- **`supabase_migrations.schema_migrations` vazia** — migrations aplicadas via SQL Editor; usar `supabase/diagnostics/`.
- **TESTES_HUMANOS.md** não é versionável.
- **Riscos aceitos** (detalhados em `docs/MEMORY-HISTORY.md`): busca textual na URL; cache em `localStorage`; envio de NF/Fatura ao Gemini; arquivos órfãos em `financial-documents`; arquivos de negócio em `public/downloads` (V-05); self-service de oficina sem rate limit; foto ao vivo de chamados só na UI; dados do veículo no Telegram; sem auditoria de desconto; trava pós-aprovação do desconto só na UI; LGPD do nome do aprovador no histórico/XLSX; nome do motorista na tabela da sub-aba Aderência (2026-08-19 — dado operacional já exposto às mesmas funções em Veículos e no Dashboard, sem ampliar a superfície); histórico de desmarcação de tratamento por chamado (2026-08-19 — o `DELETE` apaga a autoria anterior; volume esperado baixo, marcação sem alteração de dado operacional e tabela histórica desproporcional ao ganho); link público da foto de inconformidade no XLSX (2026-08-20 — amplia a distribuição da evidência já pública, aceito em troca de apoiar a fila de tratativas; CNH, assinatura e hodômetro permanecem barrados).

## Protocolo oficial de smoke

**Comando oficial:** `npm run test:smoke`

**Objetivo:** Responder se a aplicação sobe, autentica, protege rotas e mantém a navegação crítica funcionando.

**Pré-condições ambientais:**
- Dependências instaladas.
- `.env.local` válido com `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` e `SUPABASE_SERVICE_ROLE_KEY`.
- Dados demo mínimos existentes para `admin@demo.betafleet.local` e `coordinator@demo.betafleet.local`.

**Escopo incluído:**
- Tela pública de login renderiza.
- Rota protegida redireciona usuário anônimo para login.
- Sessão autenticada (admin) chega ao Dashboard.
- Shell de Cadastros renderiza e abas mudam rota/conteúdo (admin).
- Coordinator mantém navegação responsiva nas abas de Cadastros após idle (regressão específica).

**Escopo excluído:**
- CRUD completo, OCR, upload/importação, fluxos destrutivos.
- Todos os papéis do sistema.
- Todos os módulos do menu.
- Specs em `e2e/pending/**`.
- Matriz completa de permissões.

**Conduta em falha:** Parar, registrar o teste falho com erro e evidência, corrigir antes de prosseguir com qualquer outra tarefa.

**Observação:** `npm run test:e2e` é regressão completa e não substitui o smoke; smoke não substitui regressão completa.

**Spec dedicada:** `e2e/smoke/app-smoke.spec.ts`

## Observações

- **✅ RESOLVIDO em 2026-09-11 — Parque de executores gratuitos mudou (constatado em 2026-09-11)**: a tabela de `docs/EXECUTORS.md` §4.2 foi corrigida na sessão do AAII. O documento afirmava 31 gratuitos; `opencode models` lista **7**. Vinte e quatro slugs documentados não existem mais. Entrada original preservada abaixo para contexto. `opencode models` não lista mais `deepseek-v4-flash-free`, só o `deepseek-v4-flash` pago. `docs/EXECUTORS.md` §4 ainda o traz como gratuito com AA 52; atualizar a tabela. Hoje os gratuitos com nota confirmada ≥ 50 se resumem a `muse-spark-1.2-contributor-free`; o `big-pickle` entra pelo histórico. Sem candidato elegível a estreia, a sessão de cancelamento de pagamentos rodou sem executor inédito.
- **Executor do `opencode` rodou `git stash` no working tree (2026-09-11, Etapa 8 do cancelamento de pagamentos)**: o `muse-spark-1.2-contributor-free` fez `git stash push --keep-index` + `git stash pop` para comparar warnings de lint "antes e depois". O pop restaurou tudo, sem perda; foi conferido contra o snapshot do `plan-runner`, 148 caminhos sujos intactos. É ação arriscada: há dezenas de arquivos não commitados de outras sessões no working tree. Os roteiros de executor passam a proibir explicitamente `git stash/checkout/reset/restore/clean`. Vale incluir essa proibição no modelo de prompt de `docs/EXECUTORS.md`.
- **Mensagem crua de deadlock no cancelamento (2026-09-11)**: se o aprovador cancela um extra no mesmo instante em que o Financeiro lança uma parcela, o Postgres pode devolver `deadlock detected`, que aparece cru na janela de cancelamento. Nada é gravado e tentar de novo resolve. É raro; tradução da mensagem fica para sessão futura.
- **DEV-OS-002 (DEV, placa DEV4D56, cliente BetaFleet Demo) tem parcelas acima do teto do orçamento aprovado (observado em 2026-09-11)**: `approved_cost` R$ 950,00 e parcelas não reprovadas somando R$ 1.900,00. São **duas parcelas idênticas**, ambas `installment_number = 1`, R$ 950,00, status `pago`, criadas em 2026-07-09 23:31 UTC e 2026-07-10 00:39 UTC — com cara de seed duplicado. **Nenhuma das duas tem evento `created` em `payment_installment_events`**, então não há autor registrado. A origem não foi apurada: a hipótese é que tenham entrado pelo SQL Editor (escape hatch `auth.uid() IS NULL`, que libera `fn_enforce_payment_installment_budget_cap`) ou antes de o gatilho de auditoria de INSERT existir. Anterior à sessão de Pagamentos a partir da aprovação, que não a causou. Efeito prático: a OS some do Cadastrar Pagamento (saldo negativo) e distorce totais de Pagamentos em DEV — evitá-la em testes que dependem de saldo. Só DEV; não verificado se há caso análogo em PROD. Reparo (excluir a duplicata) é decisão do usuário e não foi feito.
- **Opção `Cancelar` do `<select title="Ações">` em `src/pages/Maintenance.tsx` (~linha 848, observado em 2026-09-10)**: envia o valor literal `'Cancelar'` para `updateMaintenanceStatus`, que não é um `MaintenanceStatus` válido. Pela leitura do código deve cair no `CHECK` da coluna e exibir alerta de erro — não cancela, portanto **não** é caminho que contorne o aviso de parcelas no cancelamento. Não verificado em tela. Correção provável: encaminhar para `setOrderToCancel(o)`. Sessão própria.
- **`block`/`unblock` e `set_active` gravam no MESMO `banned_until`, sem se conhecerem (descoberto em 2026-09-08, na validação em DEV)**: a ação `block` de `create-user` (linha ~267) escreve `banned_until` e **não** toca em `profiles.active`; `set_active` (linha ~301) escreve os dois. São dois eixos independentes compartilhando um único campo de armazenamento. Duas consequências reais: (a) quem foi bloqueado por `block` aparece como `active = true` e quebra a consulta de coerência; (b) **inativar e depois reativar por `set_active` um usuário que estava bloqueado por `block` manda `ban_duration: "none"` e desfaz o bloqueio deliberado em silêncio.** Não corrigido: está fora do plano fechado desta sessão. Quem for mexer nisso decide antes se `block` e `active` devem virar um eixo só ou permanecer separados com campos distintos.
- **A Invariante 4 de `fn_profile_activation_denial_reason` é inalcançável na prática (2026-09-08)**: para inativar um `Admin Master` (rank 9) seria preciso um ator de rank > 9, que não existe, então a Invariante 3 (`ator <= alvo`) sempre dispara antes. O piso "o sistema nunca fica sem Admin Master" **está garantido**, mas pela Invariante 3, não pela 4. Verificado por consulta em DEV. É redundância deliberada de defense-in-depth — **não remover como código morto** sem entender que ela passa a valer se a tabela `role_ranks` ganhar um papel acima de `Admin Master`.
- **O servidor MCP de PROD é somente-leitura, o de DEV não (constatado em 2026-09-08)**: `supabase` roda com `--read-only` (`--project-ref=oajfjdadcicgoxrfrnny`), `supabase-dev` não. Configuração em `~/.claude.json`, escopo de usuário — **não existe essa opção no painel do Supabase**. Consequência: um agente aplica migration em DEV pelo MCP, mas em PROD o caminho é o SQL Editor (que é a convenção do projeto de qualquer forma). A trava funcionou na prática nesta sessão. Segunda consequência, menos óbvia: depois do `REVOKE` da Etapa 3, a conexão somente-leitura **perdeu** o `EXECUTE` em `fn_profile_activation_denial_reason` (ela dependia do grant de `PUBLIC`), então testes dessa função em PROD só rodam pelo SQL Editor, que conecta como `postgres`.
- **Schema drift descoberto (2026-09-07): `operational_units` tem `UNIQUE (client_id, code)` em PROD (`operational_units_client_code_unique`) e essa constraint NÃO existe em DEV.** Descoberto quando a migration `20260907000001` falhou em PROD com `23505 duplicate key` — o `UPDATE` tinha sido validado só contra DEV. Consequência prática: **migrations que tocam `operational_units.code` precisam ser escritas assumindo a constraint de PROD como o caso mais restritivo**, mesmo quando testadas primeiro em DEV. Vale conferir se há outras divergências de constraint entre os dois bancos antes da próxima migration nessa tabela.
- Observações técnicas e dívidas registradas até 2026-09-01 foram arquivadas em `docs/MEMORY-HISTORY.md` → `## Arquivamento — 2026-09-01 (observações técnicas)`.

## Referência Histórica

- Histórico completo de sessões, bugs corrigidos e decisões arquivadas: `docs/MEMORY-HISTORY.md`.
- Arquivamentos anteriores: `## Arquivamento — 2026-08-19`, `## Arquivamento — 2026-06-14`, `## Arquivamento — 2026-06-19`, `## Arquivamento — 2026-07-12 (pré Pagamentos Extras)`, `## Arquivamento — 2026-07-30`.
