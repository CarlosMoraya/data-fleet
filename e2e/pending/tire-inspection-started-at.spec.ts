import { test } from '@playwright/test';

test.describe.skip('Tire inspection started_at at KM confirmation', () => {
  test('online inspection records Início as the KM confirmation moment', async () => {
    // Blocked: Driver/Auditor test users must be recreated and .env.local updated.
  });

  test('offline inspection syncs started_at from the KM confirmation moment', async () => {
    // Blocked: Driver/Auditor test users must be recreated and .env.local updated.
  });
});
