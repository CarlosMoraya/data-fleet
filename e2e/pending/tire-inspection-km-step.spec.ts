import { test } from '@playwright/test';

test.describe.skip('Tire inspection KM step', () => {
  test('new inspection shows empty KM step before the blueprint', async () => {
    // Blocked: Driver/Auditor test users must be recreated and .env.local updated.
  });

  test('resumed inspection shows editable pre-filled KM step before the blueprint', async () => {
    // Blocked: Driver/Auditor test users must be recreated and .env.local updated.
  });

  test('completed inspection opens directly in read-only blueprint mode', async () => {
    // Blocked: Driver/Auditor test users must be recreated and .env.local updated.
  });
});
